name: Qualcomm Preflight Checks
on:
  pull_request:
    branches: [ main, upstream_subdir ]
  push:
    branches: [ main, upstream_subdir ]
  workflow_dispatch:

permissions:
 contents: read
 security-events: write

jobs:
  qcom-preflight-checks:
    uses: qualcomm/qcom-reusable-workflows/.github/workflows/reusable-qcom-preflight-checks-orchestrator.yml@recur-subdir
    with:
        enable-armor-checkers: true
        armor-checker-options: '{"build-script":"ci/build.sh","runs-on":{"group":"GHA-CSEPerf-prd-SelfHosted-RG","labels":["self-hosted", "cseperf-prd-u2204-arm64-xlrg-od-ephem"]}}'
